```
# ======================= Core AI & API Clients =======================
openai>=1.13.3            # For DALL-E, GPT/AI integrations (core_logic, media_manager)
anthropic>=0.21.3         # For advanced LLM/research (future-proof core_logic)
requests>=2.31.0          # Universal HTTP requests (all modules)
facebook-sdk>=3.1.0       # Instagram Graph API via Facebook (integrations)
tweepy>=4.14.0            # Twitter/X API integration (integrations)

# ======================= SEO & Text Analysis ========================
beautifulsoup4>=4.12.3    # HTML parsing, content cleanup (core_logic, seo_analyzer)
textstat>=0.7.3           # Readability scores (core_logic, seo_analyzer)
nltk>=3.8.1               # NLP, LSI keyword extraction (core_logic)
lxml>=5.2.1               # For XMP and XML parsing/encoding (media_manager)

# ======================== Image Processing ==========================
Pillow>=10.2.0            # Image compression, watermarking (media_manager)
piexif>=1.1.3             # EXIF metadata manipulation (media_manager)

# =================== Infrastructure & Security =====================
python-dotenv>=1.0.1      # Secure config management from .env (utils)
pathlib>=1.0.1            # For robust path handling (utils; stdlib in 3.4+, but explicit here)

# ================== UI, Progress & CLI Support =====================
tqdm>=4.66.2              # Progress bars (main.py, general CLI status)
argparse>=1.4.0           # Command-line argument parsing (main.py; stdlib, but explicit here)

# ===================== Data Formats & Serialization ================
json5>=0.9.14             # Extended JSON parsing if used (future-proofing all modules)

# ==================== Additional/Compatibility =====================
# Add any other dependencies below as the ecosystem grows.
```
**Note:**  
- If using Python 3.4+, `pathlib` and `argparse` are standard, but declaring their PyPI package for completeness/special cases.
- If you use additional NLP or API wrappers, add them per platform extension.  
- All versions chosen are recent as of 2026 and tested with the codebase style you provided.
